spring:
  profiles:
    active: dev
  application:
    name: ts-article
  cloud:
    nacos:
      server-addr: ${mkdir.nacos.host}:8848
      config:
        file-extension: yaml # 文件后缀名
        shared-configs: # 共享配置
          - data-id: shared-jdbc.yaml
          - data-id: shared-seata.yaml
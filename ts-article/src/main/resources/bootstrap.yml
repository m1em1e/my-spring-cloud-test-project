spring:
  profiles:
    active: dev
  application:
    name: ts-article
  cloud:
    nacos:
      server-addr: 106.52.245.23:8848
      config:
        file-extension: yaml # 文件后缀名
        shared-configs: # 共享配置
          - dataId: shared-jdbc.yaml